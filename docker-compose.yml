services:
  halpd:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - halpd-data:/data
    restart: unless-stopped

volumes:
  halpd-data:
