{
  "name": "@halp/app",
  "version": "0.1.0",
  "description": "Halp web application shell",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@halp/core": "../core"
  }
}
